import {ViewBase} from "./../../node_modules/crs-binding/crs-view-base.js";

export default class WidgetsComponent extends ViewBase {
    async connectedCallback() {
        await super.connectedCallback();
    }
}